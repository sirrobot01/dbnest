# This directory is used for embedding frontend assets during build.
# The contents are generated by `npm run build` in the frontend directory
# and copied here before Go build. They are not committed to git.
